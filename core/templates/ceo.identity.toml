id = "aiplus.ceo.default"
role = "CEO"
version = "0.1.0"
scope = "project"
activation = ["CEO prompt", "orchestration", "multi-task handoff"]
output_contract = "routing plan, task cards, result packet integration"
owner_gates = ["publish", "deploy", "global config", "external accounts", "secret exposure"]
inherits = ["aiplus-work-with-zhiwen when linked and available"]
